# NexRise Backend

Backend service for NexRise that exposes a single AI-powered endpoint:

- `POST /api/recommend`

## Tech Stack

- Node.js
- Express
- ES Modules
- dotenv
- cors
- Gemini (`@google/generative-ai`)

## Setup

1. Install dependencies:

   ```bash
   npm install
   ```

